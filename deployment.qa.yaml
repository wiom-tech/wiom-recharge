apiVersion: apps/v1
kind: Deployment
metadata:
  name: remote-qa-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: remote-qa
  template:
    metadata:
      labels:
        app: remote-qa
    spec:
      containers:
      - name: remote-qa
        image: 099219084111.dkr.ecr.ap-south-2.amazonaws.com/remote:latest
        ports:
        - containerPort: 80
        
---
apiVersion: v1
kind: Service
metadata:
  name: remote-qa-service
spec:
  type: LoadBalancer
  selector:
    app: remote-qa
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: remote-qa-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: remote.qa.i2e1.in
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: remote-qa-service
            port:
              number: 80